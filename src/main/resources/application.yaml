Spring:
  Datasource:
    url: jdbc:mysql://localhost:3306/mysql
    username: root
    password: bornbylove123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  show-sql: true
  properties:
    hibernate:
      format-sql: true
  database: mysql
  database-platform: org.hibernate.dialect.MySQLDialect